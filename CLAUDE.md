## Agent skills

### Issue tracker

Issues en PRDs leven als markdown-bestanden onder `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Vijf canonieke triage-rollen, standaard strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: één `CONTEXT.md` + `docs/adr/` in de repo-root. See `docs/agents/domain.md`.
